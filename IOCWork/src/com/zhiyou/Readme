IOC：控制反转,可以将IOC看成一个很大的容器,容器里面存放的全部是我们的对象。
控制： 是指创建对象的过程。
反转： 之前创建对象的时候,都是我们手动去new 一个对象出来,现在所有的对象都由IOC容器去创建
了,而我们使用对象的时候直接从容器中获取即可。
DI：依赖注入,一个对象里面可能会有很多的其它对象,将其他对象设置进去的过程统称为依赖注入。
(简单点理解,就是给我们创建出来的对象赋值的过程称为DI)。

IOC是依靠DI来实现的。